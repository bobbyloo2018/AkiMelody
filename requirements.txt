flask>=3.0.0,<4.0.0
yt-dlp[default]>=2026.6.9,<2027.0.0
ytmusicapi>=1.0.0,<2.0.0
requests>=2.31.0,<3.0.0
httpx>=0.27.0,<1.0.0  # async uniform artwork resolver
wikipedia-api>=0.6.0,<1.0.0
syncedlyrics>=0.5.0,<1.0.0
curl_cffi>=0.7.0
pillow>=10.0.0

# WebView2 shell (pywebview launcher) + theme detection
pywebview>=6.0.0
pythonnet>=3.0.0
darkdetect>=0.8.0
# Release builds invoke PyInstaller through the active Python interpreter.
# Keep it in the shared requirements file so PostUpdate.bat can prepare a
# complete build environment instead of failing after the application deps.
pyinstaller>=6.0.0,<7.0.0
